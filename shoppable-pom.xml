<?xml version="1.0" encoding="UTF-8"?>
<!--
  ~ Copyright (c) 2017. Drydock B.V. (founded in The Netherlands) All rights reserved.
  -->

<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>nl.shoppable</groupId>
    <artifactId>shoppable-pom</artifactId>
    <version>1.0.0</version>
    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!--<applicationName>shoppable</applicationName>-->
        <!--${applicationName} -->
        <mavenVersion>3.3.9</mavenVersion>
    </properties>
    <packaging>pom</packaging>
    <name>Shoppable POM</name>
    <description>Shoppable Maven Default POM file</description>
    <distributionManagement>
        <snapshotRepository>
            <id>repo.shoppable.nl</id>
            <url>s3://shoppable-maven-repo/snapshot</url>
        </snapshotRepository>
        <repository>
            <id>repo.shoppable.nl</id>
            <url>s3://shoppable-maven-repo/release</url>
        </repository>
    </distributionManagement>
    <repositories>
        <repository>
            <id>repo.shoppable.nl</id>
            <url>s3://shoppable-maven-repo/release</url>
        </repository>
    </repositories>
    <build>
        <sourceDirectory>src/main/java</sourceDirectory>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>versions-maven-plugin</artifactId>
                <version>2.3</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-release-plugin</artifactId>
                <version>2.5.3</version>
            </plugin>
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.1</version>
                <configuration>
                    <source>1.8</source>
                    <target>1.8</target>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-war-plugin</artifactId>
                <version>2.4</version>
                <configuration>
                    <warSourceDirectory>web</warSourceDirectory>
                    <failOnMissingWebXml>false</failOnMissingWebXml>
                    <warName>${applicationName}</warName>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.flywaydb</groupId>
                <artifactId>flyway-maven-plugin</artifactId>
                <version>4.0.3</version>
                <configuration>
                    <url>${flyway-url}</url>
                    <user>${flyway-user}</user>
                    <password>${flyway-pass}</password>
                </configuration>
                <dependencies>
                    <dependency>
                        <groupId>org.postgresql</groupId>
                        <artifactId>postgresql</artifactId>
                        <version>9.4.1208</version>
                    </dependency>
                </dependencies>
            </plugin>
            <plugin>
                <groupId>org.glassfish.maven.plugin</groupId>
                <artifactId>maven-glassfish-plugin</artifactId>
                <version>2.1</version>
                <configuration>
                    <glassfishDirectory>${glassfish-home}</glassfishDirectory>
                    <terse>false</terse>
                    <echo>true</echo>
                    <debug>true</debug>
                    <user>${glassfish-user}</user>
                    <adminPassword>${glassfish-pass}</adminPassword>
                    <domain>
                        <name>${project.build.finalName}</name>
                        <host>${glassfish-host}</host>
                        <adminPort>${glassfish-admin-port}</adminPort>
                        <httpPort>${glassfish-port}</httpPort>
                        <reuse>false</reuse>
                    </domain>
                    <components>
                        <component>
                            <name>${applicationName}</name>
                            <artifact>${project.build.directory}/${applicationName}.war</artifact>
                        </component>
                    </components>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-clean-plugin</artifactId>
                <version>3.0.0</version>
            </plugin>
            <plugin>
                <groupId>org.apache.tomcat.maven</groupId>
                <artifactId>tomcat7-maven-plugin</artifactId>
                <version>2.2</version>
                <configuration>
                    <server>TomcatServer</server>
                    <path>/${applicationName}</path>
                    <url>${tomcat-url}</url>
                    <username>${tomcat-user}</username>
                    <password>${tomcat-pass}</password>
                    <warFile>${project.build.directory}/${applicationName}.war</warFile>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-assembly-plugin</artifactId>
                <configuration>
                    <archive>
                        <manifest>
                            <addClasspath>true</addClasspath>
                            <mainClass>co.drydock.core.indexers.OldProductRemover</mainClass>
                        </manifest>
                    </archive>
                    <descriptorRefs>
                        <descriptorRef>jar-with-dependencies</descriptorRef>
                    </descriptorRefs>
                </configuration>
                <executions>
                    <execution>
                        <id>make-assembly</id> <!-- this is used for inheritance merges -->
                        <phase>package</phase> <!-- bind to the packaging phase -->
                        <goals>
                            <goal>single</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
        <extensions>
            <extension>
                <groupId>org.kuali.maven.wagons</groupId>
                <artifactId>maven-s3-wagon</artifactId>
                <version>1.2.1</version>
            </extension>
        </extensions>
    </build>
</project>
